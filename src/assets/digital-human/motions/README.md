# Live2D motion clips (`.motion3.json`)

Owned by the Live2D runtime. Kept separate from `animations/` (which is
runtime-agnostic) because the Cubism SDK expects motions under a
dedicated folder referenced from `model.model3.json`.
