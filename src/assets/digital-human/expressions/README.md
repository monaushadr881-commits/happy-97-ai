# Shared expression presets

Maps HAPPY's `AvatarExpression` tokens to runtime-specific weight
tables. See `src/components/digital-human/renderers/expression-table.ts`
for the schema. Empty until real assets ship.
